# APP 内 Markdown 写作

点击首页顶部的写作（加号）按钮，登录 Matters 账户后开始编辑。此功能替代原来跳转网页的新文章入口。

- 输入标题及 Markdown 正文，支持标题、粗体、斜体、引用、列表、代码块、链接和 HTTPS 图片链接。提供常用语法快捷按钮与排版预览。原始 HTML 作为文字处理，不执行脚本；暂不支持表格扩展和本地图片上传。
- 每个账户保留一份本机原稿，编辑时自动保存，重启可恢复。点击“新建”会要求确认清空本机原稿，不删除云端草稿。卸载应用会丢失本机原稿。
- “保存云端”将 Markdown 转换为 HTML，通过官方 putDraft 接口保存草稿，不发布文章；原始 Markdown 留在本机。其他设备或网页草稿暂不导入本机，网页修改同一草稿会触发版本冲突保护，可通过“网页草稿”处理。
- “发布”显示标题、当前账户和公开发布确认，然后调用官方 publishArticle 接口。不会替用户自动发布。发布中或请求结果不明时锁定编辑及再次发布，使用“查询发布状态”确认结果。必要时可前往网页草稿查看。
- 本机原稿按账户隔离；请求使用开始操作时的账户令牌，不随账户切换改变。保存失败不会清空原稿。
- 预览使用离线 WebView，禁用 JavaScript、文件访问和网络加载，因此不会加载外部图片或打开链接；图片 URL 仍会保留在提交正文中。

目前正文上限 100,000 个 UTF-16 字符，标题上限 100 个；发布采用网站默认许可，不发布至 ISCN，不包含付费圈子、定时发布、封面上传等高级设置。

验证：31 项 Release 单元测试通过（含 8 项新增写作测试），Lint 0 错误。生产接口字段及参数使用 @skip 校验通过，未在真实账户创建草稿或发布文章。尚未完成真机编辑、键盘和真实账户发布验收。
